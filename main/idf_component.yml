## IDF Component Manager Manifest File
dependencies:
  espressif/esp-dsp: "*"
#  espressif/esp_io_expander_tca95xx_16bit: "==1.0.1"
  espressif/esp_lcd_panel_io_additions: "^1.0.1"
  espressif/cmake_utilities: "^0.5.2"
  ## Required IDF version
  idf:
    version: ">=4.1.0"
  # # Put list of dependencies here
  # # For components maintained by Espressif:
  # component: "~1.0.0"
  # # For 3rd party components:
  # username/component: ">=1.0.0,<2.0.0"
  # username2/component2:
  #   version: "~1.0.0"
  #   # For transient dependencies `public` flag can be set.
  #   # `public` flag doesn't have an effect dependencies of the `main` component.
  #   # All dependencies of `main` are public by default.
  #   public: true
  # esp_lcd_ili9341: "^1.0"
  esp_lcd_gc9a01: "==1.0.0"
  lvgl/lvgl: "*"
  # esp_lcd_touch_stmpe610: "^1.0"
  # espressif/esp_lcd_st7796: "^1.0"
  # atanisoft/esp_lcd_ili9488: "~1.0.0"
  #  espressif/esp_jpeg: "^1.0.5~1"
#  espressif/esp_tinyusb: "*"
#  espressif/tinyusb: '^0.15.0~5'